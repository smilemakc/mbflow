# Server Configuration
PORT=8181
HOST=0.0.0.0
READ_TIMEOUT=15s
WRITE_TIMEOUT=15s
SHUTDOWN_TIMEOUT=30s
CORS_ENABLED=true

# Database Configuration
DATABASE_URL=postgres://mbflow:mbflow@localhost:5566/mbflow?sslmode=disable
DB_MAX_CONNECTIONS=20
DB_MIN_CONNECTIONS=5
DB_MAX_IDLE_TIME=30m
DB_MAX_CONN_LIFETIME=1h

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_POOL_SIZE=10

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# Security Configuration
API_KEYS=your-api-key-1,your-api-key-2

# Optional: For production deployments
# DATABASE_URL=postgres://user:password@prod-db:5432/mbflow?sslmode=require
# REDIS_URL=redis://:password@prod-redis:6379
