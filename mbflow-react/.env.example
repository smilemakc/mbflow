# MBFlow React Frontend Configuration

# Backend URL (for Vite proxy in development)
# Default: http://localhost:8585
VITE_BACKEND_URL=http://localhost:8585

# API URL (for direct API calls in production, bypassing proxy)
# Leave empty to use relative paths (proxied in development)
# VITE_API_URL=https://api.example.com/api/v1

# WebSocket URL (for direct WS connections in production)
# Leave empty to use relative paths (proxied in development)
# VITE_WS_URL=wss://api.example.com/ws

# Development server port
# Default: 3435
VITE_PORT=3435

# Development server host
# Default: 0.0.0.0
VITE_HOST=0.0.0.0
